#include<stdio.h>

int main(){
    float arm,area; 
     printf("Enter the arm value\n");
     scanf("%f",&arm);
     area=arm*arm; 
     printf("your area result is=%.2f",area);
     return 0;
}